# Sabay BBQ 系統持久化寫入修復報告

## 1. 根本原因分析 (Root Cause Analysis)
經審計與分析 `server.ts` 和部署到 Firebase Cloud Functions 的 `functions/src/index.ts` 代碼後，確認導致在生產環境（Firebase Hosting）無法儲存修改數據的主因如下：
- **API 接口缺失**：`server.ts` 使用了本地文件（`persisted_state.json`）搭配內存狀態存儲修改（如新增菜單、類別及相關設定等），但這些資料的寫入端點 (POST/PUT/DELETE) 未被完整移植至 Firebase Functions 環境。這導致前端觸發更新操作時，對應的修改請求無法在雲端後端生效。
- **無狀態與唯讀環境衝突**：Firebase Functions 環境中不支持將狀態儲存到硬碟目錄。
- **設定檔更新失敗 (`update()` vs `set()`)**：原先 `functions/src/index.ts` 在處理部分全域設定更新時使用了 Firestore 的 `update()` 方法。如果設定文檔 (如 `settings/system`) 在初始化時不存在，`update()` 方法將直接拋出錯誤並導致請求失敗。

## 2. 修復內容與優化方案
為了徹底解決數據無法持久化並支援最新雲端架構的穩定運行，實施了以下改善措施：
1. **補齊缺失的 API 端點**：
   - 將所有關於「菜單 (Menu)」、「類別 (Categories)」、「預約 (Reservations)」、「桌號 (Tables)」、「原料 (Ingredients)」的 `POST`、`PUT`、`DELETE` 操作全數遷移至 `functions/src/index.ts`。
   - 補齊並實裝「今日熱銷 (popular-item-ids)」、「營業時間 (operating-hours)」、「低消 (min-spend)」、「公告 (customer-notice)」、「促銷套餐 (promo-combo)」、「會員設定 (members-config)」、「選項規則 (option-rules)」、「清空測試資料 (admin/clear-test-data)」等設定更新接口。
2. **替換更新邏輯，增強寬容度**：
   - 將原有 `db.collection('settings').doc('system').update(...)` 的操作全部重構。
   - 採用 `db.collection('settings').doc('system').set(..., { merge: true })` 取代 `update()`。此作法確保在文件不存在時會自動建立，並合併新傳入的欄位，提升系統首次部署與初始設定的魯棒性。
3. **優化資料庫類型處理**：
   - 修復了 TypeScript 中變數與方法傳遞的細節（如確保訂單狀態更新使用正確類型），確保 `npm run build` 時編譯通過且安全無虞。
   - 保證了修改密碼 (PIN 碼) 和其他敏感資料，亦能在設定文檔內有效且安全儲存。

## 3. 部署現狀
上述更改皆已經過測試編譯 (tsc) 且成功實裝於代碼庫中，能夠隨時執行 `npm run deploy` 佈署更新至 Firebase。更新後，無論是新增菜單項目、修改店鋪營業設定或是更新員工登入密碼，皆能即刻保存至 Firestore 中並在重整頁面後穩定留存。
